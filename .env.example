# Optional. Defaults to ./data/activity.sqlite when omitted.
SQLITE_PATH=./data/activity.sqlite

# Optional. Keep false unless a trusted reverse proxy overwrites X-Forwarded-For/X-Real-IP.
TRUST_PROXY_HEADERS=false
